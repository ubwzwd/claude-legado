---
wave: 2
depends_on: ["04-01-PLAN.md"]
files_modified:
  - "src/novel/commands.py"
  - "src/novel/state.py"
autonomous: true
---

# Phase 04-02: Book Info & Shelf Add

<objective>
Implement `/novel add <index>` to save a book from search results, and `/novel info` to view details.
</objective>

<threat_model>
- Directory traversal: No user paths processed.
- Data integrity: JSON state writing must use atomic replacements.
</threat_model>

<requirements>
- FLOW-02: Book info extraction (fetch, parse ruleBookInfo)
- FLOW-05: Add book to shelf and fetch initial TOC
</requirements>

<tasks>

<task id="04-02-01" name="Update state management for shelf">
<read_first>
- src/novel/state.py
</read_first>
<action>
Add `save_shelf(books: list[dict])` and `load_shelf() -> list[dict]` to `src/novel/state.py`.
The shelf should be saved to `~/.claude-legado/shelf.json` using atomic writes.
Ensure shelf list can be appended to safely.
Add `set_active_book(book_id: str)` to mark current reading book in `state.json`.
</action>
<acceptance_criteria>
- `grep -q "def save_shelf" src/novel/state.py` succeeds
- `grep -q "shelf.json" src/novel/state.py` succeeds
</acceptance_criteria>
</task>

<task id="04-02-02" name="Wire up /novel add and /novel shelf">
<read_first>
- src/novel/commands.py
- .planning/phases/04-read-pipeline/04-CONTEXT.md
</read_first>
<action>
Update `_add_source` (for `add`) and `_list_shelf` (for `shelf`) in `commands.py`.
For `add <index>`:
1. Load `state.load_search_cache()`. Get item at `int(index) - 1`.
2. Fetch the item's book detail page (`bookUrl`).
3. Evaluate `ruleBookInfo` fields (intro, author, cover). If missing, set `[No intro extracted]`.
4. Append to shelf via `state.load_shelf()` -> append -> `state.save_shelf()`.
5. Display success message.
For `shelf`:
1. Iterate `state.load_shelf()` and print list with `[index] Title - Author` (D-06).
2. Mark active book based on `state.json`.
</action>
<acceptance_criteria>
- `cat src/novel/commands.py` contains `load_search_cache` in `_add_source`
- `pytest tests/test_04_read_pipeline.py -k test_book_info_and_add` passing
</acceptance_criteria>
</task>

<task id="04-02-03" name="Wire up /novel info and /novel read">
<read_first>
- src/novel/commands.py
</read_first>
<action>
Implement `/novel info` (D-03) and `/novel read <id>` (D-07).
For `info`: Show details of active book from shelf (Title, Author, Intro). 
For `read <id>`: Set the active book in `state.json` based on shelf index.
</action>
<acceptance_criteria>
- `cat src/novel/commands.py` contains logic for `info` and `read`
</acceptance_criteria>
</task>

</tasks>

<must_haves>
- Missing optional data fallback (D-09) must prevent exceptions and use placeholder text.
</must_haves>

<verification>
Automated:
- Unit test for shelf insertion and info loading flow using respx mocks.
</verification>
