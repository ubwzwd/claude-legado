---
wave: 1
depends_on: []
files_modified:
  - "src/novel/commands.py"
  - "src/novel/state.py"
autonomous: true
---

# Plan 05-02: Bookshelf UX

<objective>
Enhance `/novel shelf` to show reading progress and `/novel toc` to show an active chapter marker.
</objective>

<requirements>
- Phase-5-Hardening
</requirements>

<must_haves>
- Shelf displays `Current / Total` chapters.
- TOC highlights the current chapter with `-> ` or `*`.
- Keep state correct across `state.json` and `shelf.json`.
</must_haves>

---

## Tasks

```xml
<task id="store_progress_in_shelf">
<read_first>
- src/novel/state.py
- src/novel/commands.py
</read_first>
<action>
In `src/novel/state.py`, modify `set_active_book(book_id)`:
1. `shelf = load_shelf()`
2. Find the book by `name == book_id`.
3. Set `state['chapter_index'] = book.get('chapter_index', 0)` instead of `0`.

In `src/novel/commands.py` inside `_fetch_and_stream`:
When updating `state['chapter_index'] = new_index`, ALSO:
1. `book['chapter_index'] = new_index`
2. Save the updated shelf via `save_shelf(shelf)` (you might need to reload shelf or just update the variable and save).
   *Wait, `book` is from `_get_active_book_from_shelf(state)`. You'll need to modify the actual shelf and save it.*
   Correct implementation:
   ```python
   shelf = load_shelf()
   for b in shelf:
       if b.get('name') == state.get('current_book'):
           b['chapter_index'] = new_index
           break
   save_shelf(shelf)
   ```
</action>
<acceptance_criteria>
- `grep "b\['chapter_index'\] = new_index" src/novel/commands.py` returns 0
- `src/novel/state.py` contains `state['chapter_index'] = book.get('chapter_index', 0)`
</acceptance_criteria>
</task>

<task id="format_shelf_display">
<read_first>
- src/novel/commands.py
</read_first>
<action>
In `src/novel/commands.py`, modify `_list_shelf`:
For each book, extract:
`ch_idx = book.get('chapter_index', 0) + 1`
`total = len(book.get('chapters', []))`
Format string should include `(Chapter {ch_idx} / {total})` at the end of the line if `total > 0`, otherwise `(Chapter {ch_idx})`.
Keep the active marker `*`.
</action>
<acceptance_criteria>
- `src/novel/commands.py` contains `(Chapter {ch_idx} / {total})` or similar in `_list_shelf` logic.
</acceptance_criteria>
</task>

<task id="format_toc_display">
<read_first>
- src/novel/commands.py
</read_first>
<action>
In `src/novel/commands.py`, modify the `_show_toc` display loop:
Before `print(f"[{i + 1}] ...")`, check if `book.get('name') == current_book_id` AND `i == state.get('chapter_index', 0)`.
If true, prefix the line with a marker, e.g. `-> [{i + 1}] {ch.get('name')}`.
Else, use standard `   [{i + 1}] {ch.get('name')}`.
</action>
<acceptance_criteria>
- `src/novel/commands.py` checking `i == state.get('chapter_index'` inside `_show_toc` loop.
</acceptance_criteria>
</task>
```
