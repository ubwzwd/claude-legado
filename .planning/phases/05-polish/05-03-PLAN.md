---
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - "README.md"
autonomous: true
---

# Plan 05-03: README and Setup Documentation

<objective>
Provide a comprehensive `README.md` so that a developer with no prior context can install dependencies, set up the skill, and start reading within five minutes.
</objective>

<requirements>
- Phase-5-Hardening
</requirements>

<must_haves>
- Installation steps (`pip install -e .`)
- Usage walkthrough (e.g., loading a source, searching, adding to shelf, reading)
- Information about how it integrates with Claude Code
</must_haves>

---

## Tasks

```xml
<task id="create_readme">
<read_first>
- README.md
</read_first>
<action>
Create or overwrite `README.md` at the project root with the following structure:
1. **Title and Description**: A tool to read Chinese web novels disguised as Claude AI output.
2. **Setup**:
   - Requires Python 3.12+
   - `pip install -e .`
   - Start Claude Code with the skill directory (if applicable)
3. **Quick Start**:
   - Explain how the tool uses Legado (`.json`) rules.
   - Example flow:
     - `python -m novel use ./my_source.json` or `/novel use ./my_source.json` (inside Claude Code)
     - `/novel search "Title"`
     - `/novel add 1`
     - `/novel toc`
     - `/novel read 1`
     - `/novel`, `/novel next`, `/novel prev` to read and navigate.
4. **Features**:
   - Sync terminal streaming.
   - Mimics AI thinking tokens and output generation delays.
</action>
<acceptance_criteria>
- `README.md` exists and contains "pip install -e ."
- `README.md` contains the `/novel use` and other commands documentation.
</acceptance_criteria>
</task>
```
